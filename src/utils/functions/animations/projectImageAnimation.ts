export const projectImageAnimation = (imageContainer: HTMLElement, out: boolean = false):void=>{
  imageContainer.style.transition= '0.15s ease-in-out'
  // imageContainer.style.opacity = '1'h

  if(out){
    // imageContainer.style.opacity = '0'
  }

}